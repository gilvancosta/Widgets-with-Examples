import 'package:flutter/material.dart';

class DevicePreviewWidget extends StatelessWidget {

  const DevicePreviewWidget({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Device Preview Widget'),),
           body: Container(),
       );
  }
}