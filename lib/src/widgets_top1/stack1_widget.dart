import 'package:flutter/material.dart';

class Stack1Widget extends StatelessWidget {

  const Stack1Widget({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Stack1 Widget'),),
           body: Container(),
       );
  }
}