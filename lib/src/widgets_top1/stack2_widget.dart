import 'package:flutter/material.dart';

class Stack2Widget extends StatelessWidget {

  const Stack2Widget({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Stack2 Widget'),),
           body: Container(),
       );
  }
}