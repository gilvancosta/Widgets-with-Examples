import 'package:flutter/material.dart';

class DialogsWidget extends StatelessWidget {

  const DialogsWidget({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Dialogs Widget'),),
           body: Container(),
       );
  }
}