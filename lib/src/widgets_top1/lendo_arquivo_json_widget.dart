import 'package:flutter/material.dart';

class LendoArquivoJsonWidget extends StatelessWidget {

  const LendoArquivoJsonWidget({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Lendo Arquivo Json Widget'),),
           body: Container(),
       );
  }
}