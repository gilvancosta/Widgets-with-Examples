import 'package:flutter/material.dart';

class FormulariosWidget extends StatelessWidget {

  const FormulariosWidget({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Formularios Widget'),),
           body: Container(),
       );
  }
}