import 'package:flutter/material.dart';

class BottomNavigatorBarWidget extends StatelessWidget {

  const BottomNavigatorBarWidget({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Bottom NavigatorBar Widget'),),
           body: Container(),
       );
  }
}