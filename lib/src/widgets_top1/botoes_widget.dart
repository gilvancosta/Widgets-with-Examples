import 'package:flutter/material.dart';

class BotoesWidget extends StatelessWidget {

  const BotoesWidget({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Botoes Widget '),),
           body: Container(),
       );
  }
}